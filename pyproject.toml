[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sim-plugin-pandapower"
version = "0.1.0"
description = "pandapower driver for sim."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [{ name = "Weiqi Ji", email = "jiweiqi10@gmail.com" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: Apache Software License",
]
dependencies = [
    "sim-cli-core>=0.3",
    # TODO(reviewer): pin the pandapower SDK here, e.g. "pandapower>=X.Y"
]

[project.optional-dependencies]
test = [
    "pytest>=7",
    "build>=1.0",
]

[project.entry-points."sim.drivers"]
pandapower = "sim_plugin_pandapower:PandapowerDriver"

[project.entry-points."sim.skills"]
pandapower = "sim_plugin_pandapower:skills_dir"

[project.entry-points."sim.plugins"]
pandapower = "sim_plugin_pandapower:plugin_info"

[project.urls]
Homepage = "https://github.com/svd-ai-lab/sim-plugin-pandapower"
Issues = "https://github.com/svd-ai-lab/sim-plugin-pandapower/issues"


[tool.hatch.build.targets.wheel]
packages = ["src/sim_plugin_pandapower"]
